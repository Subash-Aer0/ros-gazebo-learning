## ros2 frequently used commands

```(ros2)
ros2 service list
```

Shows the request and response type of a ros2 service
``` (ros2)
ros2 service type <service_name>
```

Shows the datatype of the request and response types of a service 
```(ros2)
ros2 interface show <type_name>
```
